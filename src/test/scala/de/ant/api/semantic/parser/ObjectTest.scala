package de.ant.api.semantic.parser

import org.scalacheck.Prop._
import org.scalacheck.Properties

object ObjectTest extends Properties("String") {

//  property("startsWith") = forAll((a: String, b: String) => (a+b).startsWith(a))

//  val endsWithProperty = property { (x: String, y: String) =>
//    (x+y) endsWith y
//  }
}